generator client {
  provider = "prisma-client"
  output   = "../app/generated/prisma"
}

datasource db {
  provider = "postgresql"
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model alerta_stock {
  id_alerta_stock           Int                     @id @default(autoincrement())
  id_stock_insumo           Int
  tipo_alerta_stock         String                  @db.VarChar(20)
  mensaje_alerta            String
  atendida                  Boolean                 @default(false)
  fecha_hora_alerta         DateTime                @default(now()) @db.Timestamp(6)
  fecha_hora_atencion       DateTime?               @db.Timestamp(6)
  id_usuario_atendio        Int?
  id_stock_insumo_ubicacion Int?
  stock_insumo              stock_insumo            @relation(fields: [id_stock_insumo], references: [id_stock_insumo], onDelete: Cascade, onUpdate: NoAction)
  stock_insumo_ubicacion    stock_insumo_ubicacion? @relation(fields: [id_stock_insumo_ubicacion], references: [id_stock_insumo_ubicacion], onDelete: NoAction, onUpdate: NoAction)
  usuario                   usuario?                @relation(fields: [id_usuario_atendio], references: [id_usuario], onDelete: NoAction, onUpdate: NoAction)

  @@index([atendida], map: "idx_alerta_stock_atendida")
  @@index([id_stock_insumo], map: "idx_alerta_stock_id_stock_insumo")
  @@index([id_stock_insumo_ubicacion], map: "idx_alerta_stock_id_stock_insumo_ubicacion")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model asignacion_pedido_usuario {
  id_asignacion_pedido_usuario Int      @id @default(autoincrement())
  id_pedido                    Int
  id_usuario                   Int
  tipo_responsabilidad_pedido  String   @db.VarChar(30)
  fecha_hora_asignacion        DateTime @default(now()) @db.Timestamp(6)
  activo                       Boolean  @default(true)
  pedido                       pedido   @relation(fields: [id_pedido], references: [id_pedido], onDelete: Cascade, onUpdate: NoAction)
  usuario                      usuario  @relation(fields: [id_usuario], references: [id_usuario], onDelete: NoAction, onUpdate: NoAction)

  @@unique([id_pedido, id_usuario, tipo_responsabilidad_pedido], map: "uq_asignacion_pedido_usuario")
  @@index([id_pedido], map: "idx_asignacion_pedido_usuario_id_pedido")
  @@index([id_usuario], map: "idx_asignacion_pedido_usuario_id_usuario")
}

model categoria_plato {
  id_categoria_plato          Int                         @id @default(autoincrement())
  nombre_categoria_plato      String                      @unique @db.VarChar(100)
  descripcion_categoria_plato String?
  activo                      Boolean                     @default(true)
  plato                       plato[]
  promocion_categoria_plato   promocion_categoria_plato[]
}

model cierre_caja {
  id_cierre_caja             Int               @id @default(autoincrement())
  fecha_operacion            DateTime          @db.Date
  turno_caja                 String            @db.VarChar(30)
  monto_inicial_caja         Decimal           @default(0) @db.Decimal(12, 2)
  total_ventas_efectivo      Decimal           @default(0) @db.Decimal(12, 2)
  total_ventas_transferencia Decimal           @default(0) @db.Decimal(12, 2)
  total_ventas_qr            Decimal           @default(0) @db.Decimal(12, 2)
  total_ventas_tarjeta       Decimal           @default(0) @db.Decimal(12, 2)
  total_cierre_caja          Decimal           @default(0) @db.Decimal(12, 2)
  fecha_hora_cierre          DateTime          @default(now()) @db.Timestamp(6)
  id_usuario_cajero          Int
  observaciones_cierre_caja  String?
  fecha_hora_apertura        DateTime?         @db.Timestamp(6)
  estado_cierre              String            @default("CERRADO") @db.VarChar(20)
  total_ingresos_manuales    Decimal           @default(0) @db.Decimal(12, 2)
  total_egresos_caja         Decimal           @default(0) @db.Decimal(12, 2)
  usuario                    usuario           @relation(fields: [id_usuario_cajero], references: [id_usuario], onDelete: NoAction, onUpdate: NoAction)
  movimiento_caja            movimiento_caja[]
}

model cliente {
  id_cliente                      Int                         @id @default(autoincrement())
  id_tipo_cliente                 Int?
  nombre_cliente                  String                      @db.VarChar(120)
  telefono_cliente                String?                     @db.VarChar(30)
  direccion_cliente               String?
  correo_electronico_cliente      String?                     @db.VarChar(150)
  fecha_hora_registro             DateTime                    @default(now()) @db.Timestamp(6)
  fecha_hora_ultima_clasificacion DateTime?                   @db.Timestamp(6)
  observaciones_cliente           String?
  tipo_cliente                    tipo_cliente?               @relation(fields: [id_tipo_cliente], references: [id_tipo_cliente], onDelete: NoAction, onUpdate: NoAction)
  cuenta_cliente                  cuenta_cliente?
  notificacion_pedido             notificacion_pedido[]
  pedido                          pedido[]
  pedido_promocion_aplicada       pedido_promocion_aplicada[]
  reserva_mesa                    reserva_mesa[]

  @@index([id_tipo_cliente], map: "idx_cliente_id_tipo_cliente")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model cuenta_cliente {
  id_cuenta_cliente                   Int       @id @default(autoincrement())
  id_cliente                          Int       @unique(map: "uq_cuenta_cliente_id_cliente")
  correo_electronico_login            String    @unique(map: "uq_cuenta_cliente_correo") @db.VarChar(150)
  password_hash                       String?   @db.VarChar(255)
  proveedor_autenticacion             String    @default("LOCAL") @db.VarChar(20)
  identificador_externo_autenticacion String?   @unique(map: "uq_cuenta_cliente_identificador_externo") @db.VarChar(255)
  telefono_login                      String?   @db.VarChar(30)
  acepto_terminos                     Boolean   @default(false)
  fecha_hora_aceptacion_terminos      DateTime? @db.Timestamp(6)
  activo                              Boolean   @default(true)
  fecha_hora_creacion                 DateTime  @default(now()) @db.Timestamp(6)
  ultima_fecha_hora_login             DateTime? @db.Timestamp(6)
  cliente                             cliente   @relation(fields: [id_cliente], references: [id_cliente], onDelete: Cascade, onUpdate: NoAction)
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model detalle_pedido {
  id_detalle_pedido                   Int                               @id @default(autoincrement())
  id_pedido                           Int
  id_plato                            Int
  cantidad_plato                      Int
  precio_unitario_venta               Decimal                           @db.Decimal(12, 2)
  descuento_linea                     Decimal                           @default(0) @db.Decimal(12, 2)
  subtotal_linea                      Decimal                           @db.Decimal(12, 2)
  estado_detalle_pedido               String                            @default("PENDIENTE") @db.VarChar(30)
  tiempo_estimado_preparacion_minutos Int?
  fecha_hora_inicio_preparacion       DateTime?                         @db.Timestamp(6)
  fecha_hora_listo                    DateTime?                         @db.Timestamp(6)
  fecha_hora_entrega_mesa             DateTime?                         @db.Timestamp(6)
  observaciones_detalle_pedido        String?
  pedido                              pedido                            @relation(fields: [id_pedido], references: [id_pedido], onDelete: Cascade, onUpdate: NoAction)
  plato                               plato                             @relation(fields: [id_plato], references: [id_plato], onDelete: NoAction, onUpdate: NoAction)
  historial_estado_detalle_pedido     historial_estado_detalle_pedido[]
  movimiento_stock_insumo             movimiento_stock_insumo[]

  @@index([estado_detalle_pedido], map: "idx_detalle_pedido_estado")
  @@index([id_pedido], map: "idx_detalle_pedido_id_pedido")
  @@index([id_plato], map: "idx_detalle_pedido_id_plato")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model factura {
  id_factura         Int      @id @default(autoincrement())
  id_pedido          Int      @unique
  numero_factura     String   @unique @db.VarChar(50)
  subtotal_factura   Decimal  @db.Decimal(12, 2)
  descuento_factura  Decimal  @default(0) @db.Decimal(12, 2)
  impuesto_factura   Decimal  @db.Decimal(12, 2)
  total_factura      Decimal  @db.Decimal(12, 2)
  fecha_hora_emision DateTime @default(now()) @db.Timestamp(6)
  id_usuario_cajero  Int
  pedido             pedido   @relation(fields: [id_pedido], references: [id_pedido], onDelete: NoAction, onUpdate: NoAction)
  usuario            usuario  @relation(fields: [id_usuario_cajero], references: [id_usuario], onDelete: NoAction, onUpdate: NoAction)
  pago               pago[]

  @@index([id_pedido], map: "idx_factura_id_pedido")
}

model historial_estado_delivery {
  id_historial_estado_delivery Int             @id @default(autoincrement())
  id_pedido                    Int
  estado_anterior_delivery     String?         @db.VarChar(30)
  estado_nuevo_delivery        String          @db.VarChar(30)
  fecha_hora_cambio            DateTime        @default(now()) @db.Timestamp(6)
  id_usuario_responsable       Int?
  observaciones_cambio         String?
  pedido_delivery              pedido_delivery @relation(fields: [id_pedido], references: [id_pedido], onDelete: Cascade, onUpdate: NoAction)
  usuario                      usuario?        @relation(fields: [id_usuario_responsable], references: [id_usuario], onDelete: NoAction, onUpdate: NoAction)

  @@index([id_pedido], map: "idx_historial_estado_delivery_id_pedido")
}

model historial_estado_detalle_pedido {
  id_historial_estado_detalle_pedido Int            @id @default(autoincrement())
  id_detalle_pedido                  Int
  estado_anterior_detalle            String?        @db.VarChar(30)
  estado_nuevo_detalle               String         @db.VarChar(30)
  fecha_hora_cambio                  DateTime       @default(now()) @db.Timestamp(6)
  id_usuario_responsable             Int?
  observaciones_cambio               String?
  detalle_pedido                     detalle_pedido @relation(fields: [id_detalle_pedido], references: [id_detalle_pedido], onDelete: Cascade, onUpdate: NoAction)
  usuario                            usuario?       @relation(fields: [id_usuario_responsable], references: [id_usuario], onDelete: NoAction, onUpdate: NoAction)

  @@index([id_detalle_pedido], map: "idx_historial_estado_detalle_pedido_id_detalle")
}

model historial_estado_pedido {
  id_historial_estado_pedido Int      @id @default(autoincrement())
  id_pedido                  Int
  estado_anterior_pedido     String?  @db.VarChar(30)
  estado_nuevo_pedido        String   @db.VarChar(30)
  fecha_hora_cambio          DateTime @default(now()) @db.Timestamp(6)
  id_usuario_responsable     Int?
  observaciones_cambio       String?
  pedido                     pedido   @relation(fields: [id_pedido], references: [id_pedido], onDelete: Cascade, onUpdate: NoAction)
  usuario                    usuario? @relation(fields: [id_usuario_responsable], references: [id_usuario], onDelete: NoAction, onUpdate: NoAction)

  @@index([id_pedido], map: "idx_historial_estado_pedido_id_pedido")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model mesa {
  id_mesa            Int            @id @default(autoincrement())
  id_sector_mesa     Int
  numero_mesa        Int            @unique
  capacidad_personas Int
  estado_mesa        String         @default("LIBRE") @db.VarChar(30)
  activo             Boolean        @default(true)
  sector_mesa        sector_mesa    @relation(fields: [id_sector_mesa], references: [id_sector_mesa], onDelete: NoAction, onUpdate: NoAction)
  pedido             pedido[]
  reserva_mesa       reserva_mesa[]

  @@index([estado_mesa], map: "idx_mesa_estado_mesa")
  @@index([id_sector_mesa], map: "idx_mesa_id_sector_mesa")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model movimiento_caja {
  id_movimiento_caja     Int          @id @default(autoincrement())
  id_cierre_caja         Int?
  id_pago                Int?
  id_usuario_responsable Int?
  tipo_movimiento_caja   String       @db.VarChar(30)
  metodo_movimiento_caja String?      @db.VarChar(20)
  monto_movimiento       Decimal      @db.Decimal(12, 2)
  descripcion_movimiento String
  referencia_externa     String?      @db.VarChar(120)
  fecha_hora_movimiento  DateTime     @default(now()) @db.Timestamp(6)
  cierre_caja            cierre_caja? @relation(fields: [id_cierre_caja], references: [id_cierre_caja], onDelete: NoAction, onUpdate: NoAction)
  pago                   pago?        @relation(fields: [id_pago], references: [id_pago], onDelete: NoAction, onUpdate: NoAction)
  usuario                usuario?     @relation(fields: [id_usuario_responsable], references: [id_usuario], onDelete: NoAction, onUpdate: NoAction)

  @@index([fecha_hora_movimiento], map: "idx_movimiento_caja_fecha_hora")
  @@index([id_cierre_caja], map: "idx_movimiento_caja_id_cierre_caja")
  @@index([id_pago], map: "idx_movimiento_caja_id_pago")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model movimiento_stock_insumo {
  id_movimiento_stock_insumo Int                     @id @default(autoincrement())
  id_stock_insumo            Int
  tipo_movimiento_stock      String                  @db.VarChar(30)
  cantidad_movimiento        Decimal                 @db.Decimal(12, 3)
  stock_anterior             Decimal                 @db.Decimal(12, 3)
  stock_posterior            Decimal                 @db.Decimal(12, 3)
  motivo_movimiento          String
  id_usuario_responsable     Int?
  id_pedido                  Int?
  id_detalle_pedido          Int?
  fecha_hora_movimiento      DateTime                @default(now()) @db.Timestamp(6)
  id_stock_insumo_ubicacion  Int?
  detalle_pedido             detalle_pedido?         @relation(fields: [id_detalle_pedido], references: [id_detalle_pedido], onDelete: NoAction, onUpdate: NoAction)
  pedido                     pedido?                 @relation(fields: [id_pedido], references: [id_pedido], onDelete: NoAction, onUpdate: NoAction)
  stock_insumo               stock_insumo            @relation(fields: [id_stock_insumo], references: [id_stock_insumo], onDelete: NoAction, onUpdate: NoAction)
  stock_insumo_ubicacion     stock_insumo_ubicacion? @relation(fields: [id_stock_insumo_ubicacion], references: [id_stock_insumo_ubicacion], onDelete: NoAction, onUpdate: NoAction)
  usuario                    usuario?                @relation(fields: [id_usuario_responsable], references: [id_usuario], onDelete: NoAction, onUpdate: NoAction)

  @@index([id_stock_insumo], map: "idx_movimiento_stock_insumo_id_stock_insumo")
  @@index([id_stock_insumo_ubicacion], map: "idx_movimiento_stock_insumo_id_stock_insumo_ubicacion")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model notificacion_pedido {
  id_notificacion_pedido Int       @id @default(autoincrement())
  id_pedido              Int
  id_cliente             Int?
  canal_notificacion     String    @db.VarChar(20)
  tipo_notificacion      String    @db.VarChar(30)
  mensaje_notificacion   String
  estado_notificacion    String    @default("PENDIENTE") @db.VarChar(20)
  fecha_hora_creacion    DateTime  @default(now()) @db.Timestamp(6)
  fecha_hora_envio       DateTime? @db.Timestamp(6)
  fecha_hora_lectura     DateTime? @db.Timestamp(6)
  cliente                cliente?  @relation(fields: [id_cliente], references: [id_cliente], onDelete: NoAction, onUpdate: NoAction)
  pedido                 pedido    @relation(fields: [id_pedido], references: [id_pedido], onDelete: Cascade, onUpdate: NoAction)

  @@index([estado_notificacion], map: "idx_notificacion_pedido_estado")
  @@index([id_cliente], map: "idx_notificacion_pedido_id_cliente")
  @@index([id_pedido], map: "idx_notificacion_pedido_id_pedido")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model pago {
  id_pago                  Int               @id @default(autoincrement())
  id_factura               Int
  metodo_pago              String            @db.VarChar(20)
  monto_pagado             Decimal           @db.Decimal(12, 2)
  estado_pago              String            @default("REGISTRADO") @db.VarChar(20)
  referencia_pago          String?           @db.VarChar(100)
  codigo_operacion_externa String?           @db.VarChar(120)
  fecha_hora_pago          DateTime          @default(now()) @db.Timestamp(6)
  observaciones_pago       String?
  movimiento_caja          movimiento_caja[]
  factura                  factura           @relation(fields: [id_factura], references: [id_factura], onDelete: Cascade, onUpdate: NoAction)

  @@index([id_factura], map: "idx_pago_id_factura")
  @@index([metodo_pago], map: "idx_pago_metodo_pago")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model pedido {
  id_pedido                     Int                         @id @default(autoincrement())
  id_cliente                    Int?
  id_mesa                       Int?
  id_usuario_registro           Int?
  canal_origen_pedido           String                      @default("INTERNO") @db.VarChar(30)
  tipo_pedido                   String                      @db.VarChar(20)
  estado_pedido                 String                      @default("PENDIENTE") @db.VarChar(30)
  cantidad_personas_mesa        Int?
  subtotal_pedido               Decimal                     @default(0) @db.Decimal(12, 2)
  descuento_total_pedido        Decimal                     @default(0) @db.Decimal(12, 2)
  impuesto_total_pedido         Decimal                     @default(0) @db.Decimal(12, 2)
  costo_envio_pedido            Decimal                     @default(0) @db.Decimal(12, 2)
  total_pedido                  Decimal                     @default(0) @db.Decimal(12, 2)
  tiempo_estimado_total_minutos Int?
  fecha_hora_creacion           DateTime                    @default(now()) @db.Timestamp(6)
  fecha_hora_confirmacion       DateTime?                   @db.Timestamp(6)
  fecha_hora_inicio_preparacion DateTime?                   @db.Timestamp(6)
  fecha_hora_listo              DateTime?                   @db.Timestamp(6)
  fecha_hora_entregado          DateTime?                   @db.Timestamp(6)
  fecha_hora_cancelacion        DateTime?                   @db.Timestamp(6)
  motivo_cancelacion            String?
  observaciones_pedido          String?
  codigo_pedido                 String?                     @unique(map: "uq_pedido_codigo_pedido") @db.VarChar(30)
  asignacion_pedido_usuario     asignacion_pedido_usuario[]
  detalle_pedido                detalle_pedido[]
  factura                       factura?
  historial_estado_pedido       historial_estado_pedido[]
  movimiento_stock_insumo       movimiento_stock_insumo[]
  notificacion_pedido           notificacion_pedido[]
  cliente                       cliente?                    @relation(fields: [id_cliente], references: [id_cliente], onDelete: NoAction, onUpdate: NoAction)
  mesa                          mesa?                       @relation(fields: [id_mesa], references: [id_mesa], onDelete: NoAction, onUpdate: NoAction)
  usuario                       usuario?                    @relation(fields: [id_usuario_registro], references: [id_usuario], onDelete: NoAction, onUpdate: NoAction)
  pedido_delivery               pedido_delivery?
  pedido_promocion_aplicada     pedido_promocion_aplicada[]

  @@index([estado_pedido], map: "idx_pedido_estado_pedido")
  @@index([fecha_hora_creacion], map: "idx_pedido_fecha_hora_creacion")
  @@index([id_cliente], map: "idx_pedido_id_cliente")
  @@index([id_mesa], map: "idx_pedido_id_mesa")
  @@index([id_usuario_registro], map: "idx_pedido_id_usuario_registro")
  @@index([tipo_pedido], map: "idx_pedido_tipo_pedido")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model pedido_delivery {
  id_pedido                        Int                         @id
  nombre_cliente_delivery          String                      @db.VarChar(120)
  telefono_entrega                 String                      @db.VarChar(30)
  direccion_entrega                String
  referencia_entrega               String?
  latitud_entrega                  Decimal?                    @db.Decimal(10, 7)
  longitud_entrega                 Decimal?                    @db.Decimal(10, 7)
  distancia_entrega_km             Decimal?                    @db.Decimal(10, 2)
  costo_envio_calculado            Decimal                     @default(0) @db.Decimal(12, 2)
  estado_delivery                  String                      @default("PENDIENTE") @db.VarChar(30)
  id_usuario_repartidor            Int?
  fecha_hora_asignacion_repartidor DateTime?                   @db.Timestamp(6)
  fecha_hora_salida_delivery       DateTime?                   @db.Timestamp(6)
  fecha_hora_entrega_final         DateTime?                   @db.Timestamp(6)
  historial_estado_delivery        historial_estado_delivery[]
  pedido                           pedido                      @relation(fields: [id_pedido], references: [id_pedido], onDelete: Cascade, onUpdate: NoAction)
  usuario                          usuario?                    @relation(fields: [id_usuario_repartidor], references: [id_usuario], onDelete: NoAction, onUpdate: NoAction)

  @@index([estado_delivery], map: "idx_pedido_delivery_estado_delivery")
  @@index([id_usuario_repartidor], map: "idx_pedido_delivery_repartidor")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model pedido_promocion_aplicada {
  id_pedido_promocion_aplicada Int       @id @default(autoincrement())
  id_pedido                    Int
  id_promocion                 Int
  id_cliente                   Int?
  monto_descuento_aplicado     Decimal   @default(0) @db.Decimal(12, 2)
  fecha_hora_aplicacion        DateTime  @default(now()) @db.Timestamp(6)
  cliente                      cliente?  @relation(fields: [id_cliente], references: [id_cliente], onDelete: NoAction, onUpdate: NoAction)
  pedido                       pedido    @relation(fields: [id_pedido], references: [id_pedido], onDelete: Cascade, onUpdate: NoAction)
  promocion                    promocion @relation(fields: [id_promocion], references: [id_promocion], onDelete: NoAction, onUpdate: NoAction)
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model plato {
  id_plato                          Int               @id @default(autoincrement())
  id_categoria_plato                Int
  nombre_plato                      String            @unique @db.VarChar(120)
  descripcion_plato                 String?
  precio_venta                      Decimal           @db.Decimal(12, 2)
  imagen_url_plato                  String?
  disponible                        Boolean           @default(true)
  tiempo_preparacion_minimo_minutos Int               @default(15)
  tiempo_preparacion_maximo_minutos Int               @default(30)
  es_preparado_en_cocina            Boolean           @default(true)
  detalle_pedido                    detalle_pedido[]
  categoria_plato                   categoria_plato   @relation(fields: [id_categoria_plato], references: [id_categoria_plato], onDelete: NoAction, onUpdate: NoAction)
  promocion_plato                   promocion_plato[]
  receta_plato                      receta_plato[]

  @@index([disponible], map: "idx_plato_disponible")
  @@index([id_categoria_plato], map: "idx_plato_id_categoria_plato")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model promocion {
  id_promocion              Int                         @id @default(autoincrement())
  nombre_promocion          String                      @unique @db.VarChar(120)
  descripcion_promocion     String?
  tipo_descuento            String                      @db.VarChar(20)
  valor_descuento           Decimal                     @db.Decimal(12, 2)
  tipo_aplicacion_promocion String                      @db.VarChar(30)
  monto_minimo_compra       Decimal                     @default(0) @db.Decimal(12, 2)
  es_acumulable             Boolean                     @default(false)
  aplica_automaticamente    Boolean                     @default(true)
  requiere_codigo           Boolean                     @default(false)
  codigo_promocional        String?                     @unique @db.VarChar(50)
  maximo_usos_total         Int?
  maximo_usos_por_cliente   Int?
  fecha_inicio              DateTime                    @db.Date
  fecha_fin                 DateTime                    @db.Date
  activa                    Boolean                     @default(true)
  id_usuario_administrador  Int
  pedido_promocion_aplicada pedido_promocion_aplicada[]
  usuario                   usuario                     @relation(fields: [id_usuario_administrador], references: [id_usuario], onDelete: NoAction, onUpdate: NoAction)
  promocion_categoria_plato promocion_categoria_plato[]
  promocion_dia_semana      promocion_dia_semana[]
  promocion_fecha_especial  promocion_fecha_especial[]
  promocion_plato           promocion_plato[]
  promocion_tipo_cliente    promocion_tipo_cliente[]

  @@index([activa], map: "idx_promocion_activa")
  @@index([fecha_inicio, fecha_fin], map: "idx_promocion_fechas")
}

model promocion_categoria_plato {
  id_promocion_categoria_plato Int             @id @default(autoincrement())
  id_promocion                 Int
  id_categoria_plato           Int
  categoria_plato              categoria_plato @relation(fields: [id_categoria_plato], references: [id_categoria_plato], onDelete: NoAction, onUpdate: NoAction)
  promocion                    promocion       @relation(fields: [id_promocion], references: [id_promocion], onDelete: Cascade, onUpdate: NoAction)

  @@unique([id_promocion, id_categoria_plato])
  @@index([id_promocion], map: "idx_promocion_categoria_id_promocion")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model promocion_dia_semana {
  id_promocion_dia_semana Int       @id @default(autoincrement())
  id_promocion            Int
  numero_dia_semana       Int
  promocion               promocion @relation(fields: [id_promocion], references: [id_promocion], onDelete: Cascade, onUpdate: NoAction)

  @@unique([id_promocion, numero_dia_semana])
}

model promocion_fecha_especial {
  id                          Int       @id @default(autoincrement()) @map("id_promocion_fecha_especial")
  id_promocion                Int
  fecha_especial              DateTime  @db.Date
  descripcion_fecha_especial  String?   @db.VarChar(150)
  promocion                   promocion @relation(fields: [id_promocion], references: [id_promocion], onDelete: Cascade, onUpdate: NoAction)

  @@unique([id_promocion, fecha_especial], map: "uniq_promocion_fecha_especial_db")
  @@index([fecha_especial], map: "idx_promocion_fecha_especial_fecha")
}

model promocion_plato {
  id_promocion_plato Int       @id @default(autoincrement())
  id_promocion       Int
  id_plato           Int
  plato              plato     @relation(fields: [id_plato], references: [id_plato], onDelete: NoAction, onUpdate: NoAction)
  promocion          promocion @relation(fields: [id_promocion], references: [id_promocion], onDelete: Cascade, onUpdate: NoAction)

  @@unique([id_promocion, id_plato])
  @@index([id_promocion], map: "idx_promocion_plato_id_promocion")
}

model promocion_tipo_cliente {
  id_promocion_tipo_cliente Int          @id @default(autoincrement())
  id_promocion              Int
  id_tipo_cliente           Int
  promocion                 promocion    @relation(fields: [id_promocion], references: [id_promocion], onDelete: Cascade, onUpdate: NoAction)
  tipo_cliente              tipo_cliente @relation(fields: [id_tipo_cliente], references: [id_tipo_cliente], onDelete: NoAction, onUpdate: NoAction)

  @@unique([id_promocion, id_tipo_cliente])
  @@index([id_promocion], map: "idx_promocion_tipo_cliente_id_promocion")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model receta_plato {
  id_receta_plato           Int          @id @default(autoincrement())
  id_plato                  Int
  id_stock_insumo           Int
  cantidad_insumo_requerida Decimal      @db.Decimal(12, 3)
  merma_porcentaje          Decimal      @default(0) @db.Decimal(5, 2)
  plato                     plato        @relation(fields: [id_plato], references: [id_plato], onDelete: Cascade, onUpdate: NoAction)
  stock_insumo              stock_insumo @relation(fields: [id_stock_insumo], references: [id_stock_insumo], onDelete: NoAction, onUpdate: NoAction)

  @@unique([id_plato, id_stock_insumo])
  @@index([id_plato], map: "idx_receta_plato_id_plato")
  @@index([id_stock_insumo], map: "idx_receta_plato_id_stock_insumo")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model reserva_mesa {
  id_reserva_mesa           Int      @id @default(autoincrement())
  id_mesa                   Int
  id_cliente                Int?
  id_usuario_registro       Int?
  nombre_contacto_reserva   String   @db.VarChar(120)
  telefono_contacto_reserva String?  @db.VarChar(30)
  cantidad_personas_reserva Int
  fecha_hora_reserva        DateTime @db.Timestamp(6)
  estado_reserva            String   @default("PENDIENTE") @db.VarChar(30)
  observaciones_reserva     String?
  fecha_hora_creacion       DateTime @default(now()) @db.Timestamp(6)
  cliente                   cliente? @relation(fields: [id_cliente], references: [id_cliente], onDelete: NoAction, onUpdate: NoAction)
  mesa                      mesa     @relation(fields: [id_mesa], references: [id_mesa], onDelete: NoAction, onUpdate: NoAction)
  usuario                   usuario? @relation(fields: [id_usuario_registro], references: [id_usuario], onDelete: NoAction, onUpdate: NoAction)

  @@index([fecha_hora_reserva], map: "idx_reserva_mesa_fecha_hora_reserva")
  @@index([id_mesa], map: "idx_reserva_mesa_id_mesa")
}

model rol {
  id_rol     Int       @id @default(autoincrement())
  nombre_rol String    @unique @db.VarChar(50)
  usuario    usuario[]
}

model sector_mesa {
  id_sector_mesa          Int     @id @default(autoincrement())
  nombre_sector_mesa      String  @unique @db.VarChar(100)
  descripcion_sector_mesa String?
  mesa                    mesa[]
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model stock_insumo {
  id_stock_insumo           Int                       @id @default(autoincrement())
  id_unidad_medida          Int
  nombre_insumo             String                    @unique @db.VarChar(120)
  descripcion_insumo        String?
  cantidad_stock_actual     Decimal                   @default(0) @db.Decimal(12, 3)
  cantidad_stock_minimo     Decimal                   @default(0) @db.Decimal(12, 3)
  costo_referencia_unitario Decimal                   @default(0) @db.Decimal(12, 2)
  activo                    Boolean                   @default(true)
  alerta_stock              alerta_stock[]
  movimiento_stock_insumo   movimiento_stock_insumo[]
  receta_plato              receta_plato[]
  unidad_medida             unidad_medida             @relation(fields: [id_unidad_medida], references: [id_unidad_medida], onDelete: NoAction, onUpdate: NoAction)
  stock_insumo_ubicacion    stock_insumo_ubicacion[]

  @@index([id_unidad_medida], map: "idx_stock_insumo_unidad")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model stock_insumo_ubicacion {
  id_stock_insumo_ubicacion Int                       @id @default(autoincrement())
  id_stock_insumo           Int
  id_ubicacion_inventario   Int
  cantidad_stock_actual     Decimal                   @default(0) @db.Decimal(12, 3)
  cantidad_stock_minimo     Decimal                   @default(0) @db.Decimal(12, 3)
  cantidad_stock_maximo     Decimal?                  @db.Decimal(12, 3)
  fecha_hora_actualizacion  DateTime                  @default(now()) @db.Timestamp(6)
  activo                    Boolean                   @default(true)
  alerta_stock              alerta_stock[]
  movimiento_stock_insumo   movimiento_stock_insumo[]
  stock_insumo              stock_insumo              @relation(fields: [id_stock_insumo], references: [id_stock_insumo], onDelete: Cascade, onUpdate: NoAction)
  ubicacion_inventario      ubicacion_inventario      @relation(fields: [id_ubicacion_inventario], references: [id_ubicacion_inventario], onDelete: NoAction, onUpdate: NoAction)

  @@unique([id_stock_insumo, id_ubicacion_inventario], map: "uq_stock_insumo_ubicacion")
  @@index([id_stock_insumo], map: "idx_stock_insumo_ubicacion_id_stock_insumo")
  @@index([id_ubicacion_inventario], map: "idx_stock_insumo_ubicacion_id_ubicacion")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model tipo_cliente {
  id_tipo_cliente            Int                      @id @default(autoincrement())
  nombre_tipo_cliente        String                   @unique @db.VarChar(50)
  descripcion_tipo_cliente   String?
  criterio_clasificacion     String                   @default("CANTIDAD_PEDIDOS") @db.VarChar(30)
  valor_minimo_clasificacion Int                      @default(0)
  valor_maximo_clasificacion Int?
  periodo_dias_evaluacion    Int                      @default(30)
  prioridad_tipo_cliente     Int                      @default(1)
  activo                     Boolean                  @default(true)
  cliente                    cliente[]
  promocion_tipo_cliente     promocion_tipo_cliente[]
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model ubicacion_inventario {
  id_ubicacion_inventario          Int                      @id @default(autoincrement())
  nombre_ubicacion_inventario      String                   @unique @db.VarChar(120)
  tipo_ubicacion_inventario        String                   @db.VarChar(30)
  descripcion_ubicacion_inventario String?
  activo                           Boolean                  @default(true)
  stock_insumo_ubicacion           stock_insumo_ubicacion[]
}

model unidad_medida {
  id_unidad_medida          Int            @id @default(autoincrement())
  nombre_unidad_medida      String         @unique @db.VarChar(50)
  abreviatura_unidad_medida String         @unique @db.VarChar(10)
  stock_insumo              stock_insumo[]
}

model usuario {
  id_usuario                      Int                               @id @default(autoincrement())
  id_rol                          Int
  nombre_completo                 String                            @db.VarChar(120)
  correo_electronico              String                            @unique @db.VarChar(150)
  password_hash                   String                            @db.VarChar(255)
  telefono_usuario                String?                           @db.VarChar(30)
  activo                          Boolean                           @default(true)
  fecha_hora_creacion             DateTime                          @default(now()) @db.Timestamp(6)
  alerta_stock                    alerta_stock[]
  asignacion_pedido_usuario       asignacion_pedido_usuario[]
  cierre_caja                     cierre_caja[]
  factura                         factura[]
  historial_estado_delivery       historial_estado_delivery[]
  historial_estado_detalle_pedido historial_estado_detalle_pedido[]
  historial_estado_pedido         historial_estado_pedido[]
  movimiento_caja                 movimiento_caja[]
  movimiento_stock_insumo         movimiento_stock_insumo[]
  pedido                          pedido[]
  pedido_delivery                 pedido_delivery[]
  promocion                       promocion[]
  reserva_mesa                    reserva_mesa[]
  rol                             rol                               @relation(fields: [id_rol], references: [id_rol], onDelete: NoAction, onUpdate: NoAction)

  @@index([id_rol], map: "idx_usuario_id_rol")
}
